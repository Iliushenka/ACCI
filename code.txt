event(join) {
    player.send(texts=["Creator is Iliushenka"], switch=2);
    player.send();
}

/*
    Комментарий типа большой
*/

event(rejoin) {
    // Тут отправка сообщения игроку
    player.send("Hello, world!", 'hiii', text=[5]);
}
